config_paths:
  env: ../../env/benchmarks/mountaincar/nonstationary_fullobs.yaml
  model: ../../model/benchmarks/amt_ppo.yaml
  training: ../../training/benchmarks/mountaincar_400384.yaml
  other: ../../other/benchmark_reporting.yaml
overrides:
  env:
    env_id: MountainCar-v0
    mask_indices: []
    phase_len: 2000
    obs_shift_scale: 0.1
    reward_scale_low: 0.8
    reward_scale_high: 1.2
  model:
    algo: ppo
    policy: amt
  training:
    num_envs: 8
    horizon: 128
    total_steps: 400384
  other:
    device: cuda
    seed: 0
    wandb: false
    report: true
    report_dir: reports/benchmarks
    report_run_name: mountaincar_nonstationary_fullobs
