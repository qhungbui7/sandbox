config_paths:
  env: ../../env/benchmarks/mountaincar/stationary_partialobs.yaml
  model: ../../model/benchmarks/ff_ppo.yaml
  training: ../../training/benchmarks/mountaincar_400384.yaml
  other: ../../other/benchmark_reporting.yaml
overrides:
  env:
    env_id: MountainCar-v0
    mask_indices: [1]
    phase_len: 0
    obs_shift_scale: 0.0
    reward_scale_low: 1.0
    reward_scale_high: 1.0
  model:
    algo: ppo
    policy: ff
  training:
    num_envs: 8
    horizon: 128
    total_steps: 400384
  other:
    device: cuda
    seed: 0
    wandb: false
    report: true
    report_dir: reports/benchmarks
    report_run_name: mountaincar_stationary_partialobs_ff
