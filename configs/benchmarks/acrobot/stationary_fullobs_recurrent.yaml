config_paths:
  env: ../../env/benchmarks/acrobot/stationary_fullobs.yaml
  model: ../../model/benchmarks/recurrent_ppo.yaml
  training: ../../training/benchmarks/acrobot_300032.yaml
  other: ../../other/benchmark_reporting.yaml
overrides:
  env:
    env_id: Acrobot-v1
    mask_indices: []
    phase_len: 0
    obs_shift_scale: 0.0
    reward_scale_low: 1.0
    reward_scale_high: 1.0
  model:
    algo: ppo
    policy: recurrent
  training:
    num_envs: 8
    horizon: 128
    total_steps: 300032
  other:
    device: cuda
    seed: 0
    wandb: false
    report: true
    report_dir: reports/benchmarks
    report_run_name: acrobot_stationary_fullobs_recurrent
